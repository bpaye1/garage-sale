import React from 'react';

export default class App extends React.Component {
    render() {
        return (
            <div>React is working, and react-hot-loader is working also!</div>
        )
    }
}
